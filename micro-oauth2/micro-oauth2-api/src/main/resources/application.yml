server:
  port: 9501
spring:
  profiles:
    active: dev
  application:
    name: micro-oauth2-api
  cloud:
    nacos:
      discovery:
        server-addr: a.usts.top:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
